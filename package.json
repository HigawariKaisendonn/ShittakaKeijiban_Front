{
  "name": "shittaka-keijiban-front",
  "private": true,
  "scripts": {
    "build": "cd web && npm install && npm run build",
    "dev": "cd web && npm run dev",
    "start": "cd web && npm start",
    "install": "cd web && npm install"
  },
  "dependencies": {
    "classnames": "^2.5.1",
    "lucide-react": "^0.513.0",
    "sass": "^1.89.1"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "prettier": "^3.6.2"
  }
}
